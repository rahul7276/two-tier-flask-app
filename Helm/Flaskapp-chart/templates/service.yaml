apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-flaskapp
spec:
  selector:
    app: {{ .Release.Name }}-flaskapp
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
